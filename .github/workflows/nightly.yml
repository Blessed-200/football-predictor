name: Nightly Football Prediction

on:
  schedule:
    - cron: '6 6 * * *'      # 06:00 UTC = 22:00 PST anterior
  workflow_dispatch:

jobs:
  predict:
    runs-on: ubuntu-latest
    env:
      TELEGRAM_BOT_TOKEN: 7852899849:AAHGe4o-19s0wQThBpIqDD0gJ_F1ZctaYSw
      TELEGRAM_CHAT_ID: 8083268965
      DB_URL: sqlite:///data/predictions.db

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -r requirements.txt

      - name: Debug: Listar contenido del repositorio
        run: ls -R .

      - name: Debug: Mostrar directorio de trabajo y versión de Python
        run: |
          pwd
          python --version

      - name: Run prediction script
        run: python src/main.py
